 for (int i = 0; i < wordPlay.length(); i++) {
            wordPlay.charAt(i)
        }
        
        
        char[] holdme = wordPlay.toCharArray();
        String playWord = "";
        for (int i = 0; i < holdme.length; i++) {
            playWord = holdme[i] + playWord;
        }
        System.Out.Println("")
